op:22px!important}.asbo-account-page .woocommerce-MyAccount-content{padding:19px;border-radius:15px}.asbo-account-dashboard-hero,.asbo-account-section-heading,.asbo-account-page-header,.asbo-view-order-overview{align-items:flex-start;flex-direction:column}.asbo-account-dashboard-hero .asbo-account-primary-link{width:100%;box-sizing:border-box}.asbo-account-quick-grid{grid-template-columns:1fr 1fr;gap:8px}.asbo-account-quick-card{min-height:150px;padding:14px}.asbo-account-quick-card p{display:none}.asbo-account-order-card__top,.asbo-account-order-card__meta,.asbo-account-order-card__artwork{align-items:flex-start;flex-direction:column;gap:6px}.asbo-artwork-hub-card__heading{flex-direction:column;gap:8px}.asbo-artwork-hub-card__meta{gap:12px}.asbo-artwork-hub-card__meta span{min-width:42%}.asbo-account-page table.shop_table,.asbo-account-page table.woocommerce-table{display:block;border:0!important;background:transparent}.asbo-account-page table.shop_table thead,.asbo-account-page table.woocommerce-table thead{display:none}.asbo-account-page table.shop_table tbody,.asbo-account-page table.woocommerce-table tbody{display:grid;gap:10px}.asbo-account-page table.shop_table tr,.asbo-account-page table.woocommerce-table tr{display:block;padding:13px;border:1px solid var(--asbo-line);border-radius:12px;background:#fff}.asbo-account-page table.shop_table td,.asbo-account-page table.woocommerce-table td{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:7px 0!important;border:0!important;text-align:right!important}.asbo-account-page table.shop_table td:before,.asbo-account-page table.woocommerce-table td:before{float:none!important;color:var(--asbo-muted);font-size:10px;font-weight:850;letter-spacing:.05em;text-transform:uppercase}.asbo-account-page .woocommerce-orders-table__cell-order-actions{display:block!important;text-align:left!important}.asbo-account-page .woocommerce-Addresses,.asbo-account-page .woocommerce-customer-details .woocommerce-columns,.asbo-account-auth #customer_login{grid-template-columns:1fr}.asbo-account-auth #customer_login>.u-column1,.asbo-account-auth #customer_login>.u-column2{padding:20px!important}}
+CSS;
+    }
+}
diff --git a/includes/class-asbo-artwork-review.php b/includes/class-asbo-artwork-review.php
index b3f1bca..0cfdb99 100644
--- a/includes/class-asbo-artwork-review.php
+++ b/includes/class-asbo-artwork-review.php
@@ -103,7 +103,7 @@ final class ASBO_Artwork_Review {
         $status_label = self::status_label( $status );
         $status_class = self::status_class( $status );
         ?>
-        <section class="asbo-artwork-customer" aria-labelledby="asbo-artwork-heading-<?php echo esc_attr( $order_id ); ?>">
+        <section id="asbo-artwork" class="asbo-artwork-customer" aria-labelledby="asbo-artwork-heading-<?php echo esc_attr( $order_id ); ?>">
             <?php self::customer_styles(); ?>
 
             <div class="asbo-artwork-customer__heading">
@@ -682,7 +682,6 @@ final class ASBO_Artwork_Review {
         $actor_name = $actor && $actor->exists() ? $actor->display_name : __( 'Store administrator', 'all-star-bulk-order' );
         $now = current_time( 'mysql' );
 
-        $order->update_meta_data( self::META_STATUS, 'approved' );
         $order->delete_meta_data( self::META_REVIEW_NOTE );
         $order->update_meta_data( self::META_REVIEWER, get_current_user_id() );
         $order->update_meta_data( self::META_REVIEWED_AT, $now );
@@ -697,7 +696,9 @@ final class ASBO_Artwork_Review {
                 'actor'     => $actor_name,
             )
         );
-        $order->save();
+        if ( ! self::persist_artwork_status( $order, 'approved' ) ) {
+            self::redirect_admin_action( $order, 'error', __( 'Artwork approval could not be saved. Please reload the order and try again.', 'all-star-bulk-order' ) );
+        }
         $order->add_order_note( sprintf( __( 'Artwork approved by %s.', 'all-star-bulk-order' ), $actor_name ) );
         self::send_customer_approved_email( $order );
         self::redirect_admin_action( $order, 'approved' );
@@ -725,7 +726,6 @@ final class ASBO_Artwork_Review {
         $actor_name = $actor && $actor->exists() ? $actor->display_name : __( 'Store administrator', 'all-star-bulk-order' );
         $now = current_time( 'mysql' );
 
-        $order->update_meta_data( self::META_STATUS, 'changes_requested' );
         $order->update_meta_data( self::META_REVIEW_NOTE, $review_note );
         $order->update_meta_data( self::META_REVIEWER, get_current_user_id() );
         $order->update_meta_data( self::META_REVIEWED_AT, $now );
@@ -740,7 +740,9 @@ final class ASBO_Artwork_Review {
                 'actor'     => $actor_name,
             )
         );
-        $order->save();
+        if ( ! self::persist_artwork_status( $order, 'changes_requested' ) ) {
+            self::redirect_admin_action( $order, 'error', __( 'The artwork review status could not be saved. Please reload the order and try again.', 'all-star-bulk-order' ) );
+        }
         $order->add_order_note( sprintf( __( 'Artwork changes requested by %1$s: %2$s', 'all-star-bulk-order' ), $actor_name, $review_note ) );
         self::send_customer_changes_email( $order, $review_note );
         self::redirect_admin_action( $order, 'changes-requested' );
@@ -934,11 +936,48 @@ final class ASBO_Artwork_Review {
         echo '<span class="asbo-order-artwork-badge asbo-order-artwork-badge--' . esc_attr( $status ) . '" style="display:inline-block;padding:3px 7px;border-radius:999px;font-size:11px;font-weight:700;background:' . esc_attr( self::status_inline_background( $status ) ) . ';color:' . esc_attr( self::status_inline_color( $status ) ) . ';">' . esc_html( self::status_label( $status ) ) . '</span>';
     }
 
+    public static function get_order_artwork_status( WC_Order $order ): string {
+        return self::normalized_status( $order );
+    }
+
+    public static function get_order_artwork_status_label( WC_Order $order ): string {
+        return self::status_label( self::normalized_status( $order ) );
+    }
+
+    public static function get_order_artwork_file_count( WC_Order $order ): int {
+        return count( self::get_files( $order ) );
+    }
+
     private static function get_files( WC_Order $order ): array {
         $files = $order->get_meta( self::META_FILES, true );
         return is_array( $files ) ? $files : array();
     }
 
+    private static function persist_artwork_status( WC_Order $order, string $status ): bool {
+        $status = sanitize_key( $status );
+        if ( ! in_array( $status, array( 'awaiting_review', 'changes_requested', 'approved' ), true ) ) {
+            return false;
+        }
+
+        $order_id = $order->get_id();
+        $order->update_meta_data( self::META_STATUS, $status );
+        $order->save_meta_data();
+        $order->save();
+
+        if ( function_exists( 'wc_delete_shop_order_transients' ) ) {
+            wc_delete_shop_order_tran