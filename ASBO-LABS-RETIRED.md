# ASBO Labs retired

ASBO Labs is no longer an active release/testing channel. The beta updater feed and Labs build workflow have been removed. Historical source and packages remain on this branch only as an archive.

Future ASBO testing should use a separate staging/test WordPress environment with the normal ASBO production codebase before promotion to `main` / `latest.json`.
